# Leave unset for local dev (Vite proxies /api to the backend on :4000).
# In production, set this to your deployed backend's URL, e.g.
# VITE_API_BASE_URL=https://clarity-api.onrender.com/api
VITE_API_BASE_URL=
